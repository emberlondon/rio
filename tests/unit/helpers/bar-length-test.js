import { barLength } from 'rio/helpers/bar-length';
import { module, test } from 'qunit';

module('Unit | Helper | bar length');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = barLength([42]);
  assert.ok(result);
});
