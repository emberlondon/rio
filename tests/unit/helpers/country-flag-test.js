import { countryFlag } from 'rio/helpers/country-flag';
import { module, test } from 'qunit';

module('Unit | Helper | country flag');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = countryFlag([42]);
  assert.ok(result);
});
