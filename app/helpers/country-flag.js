import Ember from 'ember';
import isoCode from 'rio/helpers/iso-code';
import worldFlag from 'world-flag';

export function countryFlag([countryName]/*, hash*/) {
  return params;
}

export default Ember.Helper.helper(countryFlag);
